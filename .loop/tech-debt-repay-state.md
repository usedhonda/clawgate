# Loop state — 技術的負債の返済

## Budget
iteration cap 12 / no-progress streak 3 / wall-clock 120min

## Done
（まだ無し）

## Failed / blocked
（まだ無し）

## Next step
ITERATION 0: 深読みで `.loop/tech-debt-map.md` を作成（根拠ファイル:行 / 影響範囲 / リスク / 検証法 / 実施 or escalate 判定）。読み専調査は Explore サブエージェント並列可。map 完成後に `.loop/tech-debt-done.json` へ verify -> safety-net -> cleanup -> separation -> boundary の順で ledger 化し、`docs(loop):` で commit。実コード変更はまだしない。
