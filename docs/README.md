# docs

- `chrome-extension.md` — the Chrome extension: install, connect, what it captures, and the limits of each observation.
- `meeting-minutes-spec.md` — the meeting record, the minutes request envelope, and the reply the client accepts.
- `log/codex/` for implementation and handoff notes.
- `ask/chatgpt/` and `ask/gemini/` for external AI discussion artifacts.
- Add project-specific documentation under this tree as needed.
